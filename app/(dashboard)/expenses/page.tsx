// ============================================================
// Expenses & Fuel Logging — Page Stub
// Frontend team: Build expense table, form, fuel chart, cost summary card
// API: GET/POST /api/expenses, GET/PUT/DELETE /api/expenses/[id]
// ============================================================

export default function ExpensesPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-white mb-6">Expenses & Fuel Logging</h1>
      <p className="text-gray-400">
        TODO: Add ExpenseTable, ExpenseForm, FuelChart, CostSummaryCard.
        Support filtering by vehicle, expense type, and date range.
      </p>
      <p className="text-gray-500 text-sm mt-2">
        API endpoints: GET/POST /api/expenses, GET/PUT/DELETE /api/expenses/[id]
      </p>
    </div>
  );
}
