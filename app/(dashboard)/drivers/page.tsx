// ============================================================
// Driver Profiles — Page Stub
// Frontend team: Build driver table, form, safety score gauge, license badge
// API: GET/POST /api/drivers, GET/PUT/DELETE /api/drivers/[id]
// ============================================================

export default function DriversPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-white mb-6">Driver Profiles & Safety</h1>
      <p className="text-gray-400">
        TODO: Add DriverTable, DriverForm, SafetyScoreGauge, LicenseBadge.
        Show license expiry warnings and compliance status.
      </p>
      <p className="text-gray-500 text-sm mt-2">
        API endpoints: GET/POST /api/drivers, GET/PUT/DELETE /api/drivers/[id]
      </p>
    </div>
  );
}
