// ============================================================
// Vehicle Registry — Page Stub
// Frontend team: Build vehicle table with CRUD, status pills, filters
// API: GET/POST /api/vehicles, GET/PUT/DELETE /api/vehicles/[id]
// ============================================================

export default function VehiclesPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-white mb-6">Vehicle Registry</h1>
      <p className="text-gray-400">
        TODO: Add VehicleTable, VehicleFormModal, StatusPill, FilterBar.
        Support CRUD operations, filtering by status/type/region, and search.
      </p>
      <p className="text-gray-500 text-sm mt-2">
        API endpoints: GET/POST /api/vehicles, GET/PUT/DELETE /api/vehicles/[id]
      </p>
    </div>
  );
}
