// ============================================================
// Command Center (Dashboard) — Page Stub
// Frontend team: Build KPI cards, fleet status chart, active trips table
// API: GET /api/analytics?report=dashboard
// ============================================================

export default function CommandCenterPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-white mb-6">Command Center</h1>
      <p className="text-gray-400">
        TODO: Add KPI cards (Active Fleet, Maintenance Alerts, Utilization Rate, Pending Cargo),
        fleet status chart, active trips table, and alerts list.
      </p>
      <p className="text-gray-500 text-sm mt-2">
        API endpoint: GET /api/analytics?report=dashboard
      </p>
    </div>
  );
}
