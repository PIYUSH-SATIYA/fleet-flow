// ============================================================
// Maintenance & Service Logs — Page Stub
// Frontend team: Build maintenance table, service log form, vehicle health badge
// API: GET/POST /api/maintenance, GET/PUT /api/maintenance/[id]
// ============================================================

export default function MaintenancePage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-white mb-6">Maintenance & Service Logs</h1>
      <p className="text-gray-400">
        TODO: Add MaintenanceTable, ServiceLogForm, VehicleHealthBadge.
        Note: Creating a service log auto-sets the vehicle to &quot;In Shop&quot; status.
      </p>
      <p className="text-gray-500 text-sm mt-2">
        API endpoints: GET/POST /api/maintenance, GET/PUT /api/maintenance/[id]
      </p>
    </div>
  );
}
